# Assets - 静态资源

此目录存放所有静态资源文件。

## 目录结构

```
assets/
├── index.css          # 全局样式
├── images/            # 图片资源（如有）
├── fonts/             # 字体文件（如有）
└── sounds/            # 音效文件（如有）
```

## 当前文件

- `index.css` - 全局样式文件，包含赛博朋克主题的 CSS 变量和基础样式
